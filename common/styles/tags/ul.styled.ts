import { css } from "styled-components";

const ul = css`
  margin-left: 1.45rem;

  > li {
    list-style-type: disc;
    list-style-position: outside;
    margin: 0.5rem 0;
    line-height: 1.75rem;
  }
`;

export default ul;
