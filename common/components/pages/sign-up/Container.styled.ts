import styled from "styled-components";
import { Container as LogInContainer } from "@components/pages/log-in";

const Container = styled(LogInContainer)`
  width: 100%;
`;

export default Container;
