// import { compare } from "bcrypt";

// const performWithHashComp = (
//   hashes: [string, string],
//   callback: () => void
// ): void => {
//   compare(hashes[0], hashes[1], (err, result) => {
//     if(err) console.log(err);
//     else if {
//       (result) callback();
//     } 
//     else return false;
//   });
// };

// export default performWithHashComp;
